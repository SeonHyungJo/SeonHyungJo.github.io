import { styled } from '@stitches.config'

const FooterLayout = styled('footer', {
  padding: '10px 0 65px',
  textAlign: 'center',
  fontSize: '12px',

  color: '#aaa'
})

export default FooterLayout;
